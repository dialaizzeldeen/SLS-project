<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.seamlessshopping">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <uses-feature android:glEsVersion="0x00020000" />
    <uses-feature
        android:glEsVersion="0x00010001"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".profilecategory"
            android:label="@string/title_activity_profilecategory"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".timePage" />
        <activity android:name=".cart" />
        <activity android:name=".signupPage" />
        <activity android:name=".loginPage" />
        <activity android:name=".MainActivity" />
        <activity android:name=".profile" />
        <activity android:name=".productMain" />
        <activity android:name=".paymentHistory" />
        <activity android:name=".NewllyAdded">

        </activity>
        <activity android:name=".Categories_Activity">  <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>
        <activity android:name=".searching" />
<activity android:name=".resturanatActivity">
  </activity>
    </application>

</manifest>